<% provide(:title, @user.name) %>
<div class ="row">
<aside class="col-md-4">
<section class="user_info">
<h1>
	<!-- <%= gravatar_for @user %> -->
	<avatar class="img-thumbnail">
		<%= image_tag @user.profile_picture_url(:thumb).to_s %>
	</avatar>
	<%= @user.name %>
</h1>
</section>
</aside>
</div>